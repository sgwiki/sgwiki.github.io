[project]
name = "amadeus"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp>=1.0.0",
    "rdflib>=7.0.0",
]

[dependency-groups]
dev = [
    "pyshacl>=0.31.0",
    "pytest>=9.1.0",
    "ruff>=0.15.17",
]
