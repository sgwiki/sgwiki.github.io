# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Env / secrets
.env
.env.local
.env.*.local

# uv
.uv/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Node (루트 node_modules 방지; worker 의존성은 별도 관리)
node_modules/

# Temp files
docs/대화로그_tmp.md

# QA-wiki run outputs (kept locally, regenerated by run_makewiki.sh)
artifacts/

# Large local data (qaset JSON, archives, raw source collections, P6 inputs — not in git)
data/

# Reference material — local RAG 소스, 서버 불필요
reference/

# MkDocs build output
site/
docs/
# Runtime: R2 폴링 수신함, 처리 완료 마커, 결정 로그
suggestions/

# Temp
tmp/

# OMC / grepika / agent harness caches (local-only)
.omc/
.omx/
.osgrep/
.claude/
.omc-config.json
.osgrep
.admin/
.agents/
.omg/
skills-lock.json

# HolyClaude 컨테이너 런타임 상태 (머신별 로컬, 공유 금지)
# cloudcli/auth.db는 GitHub 토큰을 담음 — 절대 커밋 금지
docker/holyclaude/data/cloudcli/
docker/holyclaude/data/claude/.codex/
docker/holyclaude/data/claude/.cursor/
docker/holyclaude/data/claude/.gemini/
docker/holyclaude/data/claude/.claude.json.persist
docker/holyclaude/data/claude/.holyclaude-bootstrapped
docker/holyclaude/data/claude/CLAUDE.md.bak
docker/holyclaude/data/claude/settings.json.bak
docker/holyclaude/data/claude/project-config.json
docker/holyclaude/data/claude/plugins/
docker/holyclaude/data/claude/projects/
# Claude Code 런타임 산출물
docker/holyclaude/data/claude/sessions/
docker/holyclaude/data/claude/shell-snapshots/
docker/holyclaude/data/claude/backups/

# sg-worldline-map (인터랙티브 맵 SPA)
# src/data/*.json은 generate-data.py 산출물이나 빌드 입력이라 tracked 허용.
# dist/, node_modules/, TS 빌드 캐시는 무시.
sg-worldline-map/node_modules/
sg-worldline-map/dist/
sg-worldline-map/dist-tsbuildinfo/
sg-worldline-map/*.tsbuildinfo

# Planning/interview 산출물 (planning-with-files + omx)
.omx/
# 루트 planning 파일 (pi/omx 작업 추적용)
findings.md
task_plan.md
progress.md
progress.txt
